#include <iostream>
#include <vector>
using namespace std;
class Solution {
    public:
        vector<vector<int>> updateMatrix(vector<vector<int>> &mat) {}
};
int main() {
    // the question is find the nearest zero in the matrix
    vector<vector<int>> mat = {{0, 0, 0}, {0, 1, 0}, {0, 0, 0}};
    return 0;
}
