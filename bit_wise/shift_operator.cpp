#include <stdio.h>
#include <iostream>

using namespace std;
int main(){
    int n=4;
    // we what a mltiple of 7*n and we  wan't to do it in the efficint manner
    // what we do we do
    // we do n << 3 we get 8n
    // then we do 8n-n we get the 7n
}
