#include <bits/stdc++.h>

using namespace std;
int minjumps(int arr[], int start, int end, int n)
{
    if (arr[start] == 0)
    {
        return -1;
    }
}

int main()
{
    int n = 11;
    int arr[] = {1, 3, 5, 8, 9, 2, 6, 7, 6, 8, 9};
    minjumps(arr, 0, arr[0], n);
    cout << "hello world" << endl;
    return 0;
}